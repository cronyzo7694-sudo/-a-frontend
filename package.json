{
  "name": "pariksha-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root wrapper so Cloudflare builds the app in ./frontend",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "deploy": "npm run build && wrangler deploy"
  }
}
